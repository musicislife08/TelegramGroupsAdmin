---
phase: 12-clamav-ioptions-refactor-doc-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/compose.development.yml
  - examples/compose.production.yml
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - CLAM-01

must_haves:
  truths:
    - "Compose files use CLAMAV_HOST/CLAMAV_PORT (single underscore) matching what ClamAVScannerService reads via Environment.GetEnvironmentVariable"
    - "Both compose files include a commented ENABLE_METRICS example in the observability section"
    - "REQUIREMENTS.md marks CLAM-01 as complete and traceability table shows Complete status"
  artifacts:
    - path: "examples/compose.development.yml"
      provides: "Development compose with corrected CLAMAV env var names and ENABLE_METRICS comment"
      contains: "CLAMAV_HOST"
    - path: "examples/compose.production.yml"
      provides: "Production compose with corrected CLAMAV env var names and ENABLE_METRICS comment"
      contains: "CLAMAV_HOST"
    - path: ".planning/REQUIREMENTS.md"
      provides: "CLAM-01 marked complete"
      contains: "[x] **CLAM-01**"
  key_links:
    - from: "examples/compose.development.yml"
      to: "TelegramGroupsAdmin.ContentDetection/Services/Scanning/ClamAVScannerService.cs"
      via: "CLAMAV_HOST/CLAMAV_PORT env var names must match"
      pattern: "CLAMAV_HOST"
    - from: "examples/compose.production.yml"
      to: "TelegramGroupsAdmin.ContentDetection/Services/Scanning/ClamAVScannerService.cs"
      via: "CLAMAV_HOST/CLAMAV_PORT env var names must match"
      pattern: "CLAMAV_HOST"
---

<objective>
Fix compose file env var names to match what the code reads and add ENABLE_METRICS documentation.

Purpose: Phase 9 implemented ClamAV env var override using `Environment.GetEnvironmentVariable("CLAMAV_HOST")` and `Environment.GetEnvironmentVariable("CLAMAV_PORT")` (single underscore). The compose files incorrectly use `CLAMAV__HOST`/`CLAMAV__PORT` (double underscore, ASP.NET Core IConfiguration convention). Since the code reads raw env vars (not IConfiguration), the double-underscore names never reach the override logic. Fix compose to match code. Also document the ENABLE_METRICS env var added in Phase 11.

Output: Corrected compose files, updated REQUIREMENTS.md
</objective>

<execution_context>
@/Users/keisenmenger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/keisenmenger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-clamav-ioptions-refactor-doc-fixes/12-CONTEXT.md

@examples/compose.development.yml
@examples/compose.production.yml
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix compose env var names and add ENABLE_METRICS</name>
  <files>examples/compose.development.yml, examples/compose.production.yml</files>
  <action>
In both compose files, make these changes:

**1. Rename ClamAV env vars (single underscore to match code):**

In `examples/compose.development.yml` (lines 173-174), change:
```yaml
CLAMAV__HOST: clamav
CLAMAV__PORT: "3310"
```
to:
```yaml
CLAMAV_HOST: clamav
CLAMAV_PORT: "3310"
```

In `examples/compose.production.yml` (lines 136-137), make the same change:
```yaml
CLAMAV_HOST: clamav
CLAMAV_PORT: "3310"
```

Do NOT change the section header comment ("ClamAV Connection") — it is correct.

**2. Add ENABLE_METRICS commented example:**

In `examples/compose.development.yml`, add a new commented section AFTER the existing OTEL env vars (after line 186 `OTEL_SERVICE_NAME: TelegramGroupsAdmin`), before the Message History section:

```yaml
      # =======================================================================
      # Metrics (Optional - Standalone Prometheus /metrics endpoint)
      # =======================================================================
      # Uncomment to expose /metrics without requiring full OTEL stack:
      # ENABLE_METRICS: "true"
      # Note: OTEL_EXPORTER_OTLP_ENDPOINT above already enables /metrics implicitly
```

In `examples/compose.production.yml`, add a new section AFTER the ClamAV Connection section (after line 137 `CLAMAV_PORT: "3310"`), before the Message History section:

```yaml
      # =======================================================================
      # Observability (Optional - Prometheus Metrics)
      # =======================================================================
      # Uncomment to expose Prometheus /metrics endpoint for monitoring:
      # ENABLE_METRICS: "true"
      #
      # For full observability (structured logging + traces + metrics), also set:
      # SEQ_URL: http://seq:80
      # OTEL_EXPORTER_OTLP_ENDPOINT: http://seq:5341/ingest/otlp
      # OTEL_SERVICE_NAME: TelegramGroupsAdmin
```

**CRITICAL: No .cs files change. No tests change. No DI changes. This is docs-only per user decision.**
  </action>
  <verify>
    <automated>grep -n "CLAMAV_HOST\|CLAMAV_PORT\|CLAMAV__HOST\|CLAMAV__PORT\|ENABLE_METRICS" examples/compose.development.yml examples/compose.production.yml</automated>
  </verify>
  <done>
- Both compose files use `CLAMAV_HOST` and `CLAMAV_PORT` (single underscore, no double-underscore instances remain)
- Both compose files contain a commented `ENABLE_METRICS` example
- No other changes to compose files
  </done>
</task>

<task type="auto">
  <name>Task 2: Mark CLAM-01 complete in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
In `.planning/REQUIREMENTS.md`, make two changes:

**1. Check CLAM-01 as complete (line 22):**

Change:
```
- [ ] **CLAM-01**: `CLAMAV_HOST` and `CLAMAV_PORT` env vars override DB-stored ClamAV host and port (compose examples must use single-underscore names matching `Environment.GetEnvironmentVariable`)
```
to:
```
- [x] **CLAM-01**: `CLAMAV_HOST` and `CLAMAV_PORT` env vars override DB-stored ClamAV host and port (compose examples must use single-underscore names matching `Environment.GetEnvironmentVariable`)
```

**2. Update traceability table (line 72):**

Change:
```
| CLAM-01 | Phase 12 | Pending |
```
to:
```
| CLAM-01 | Phase 12 | Complete |
```

**3. Update "Last updated" line (line 89):**

Change date and description to reflect this update:
```
*Last updated: 2026-03-19 after Phase 12 compose env var fix*
```

No other changes to REQUIREMENTS.md.
  </action>
  <verify>
    <automated>grep -n "CLAM-01" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
- CLAM-01 checkbox is `[x]` (complete)
- Traceability table shows CLAM-01 as Complete
- All 16 v1.2 requirements now show Complete status
  </done>
</task>

</tasks>

<verification>
1. `grep -c "CLAMAV__" examples/compose.development.yml examples/compose.production.yml` — both must return 0 (no double-underscore instances)
2. `grep -c "CLAMAV_HOST\|CLAMAV_PORT" examples/compose.development.yml examples/compose.production.yml` — both must return at least 1 (single-underscore present)
3. `grep -c "ENABLE_METRICS" examples/compose.development.yml examples/compose.production.yml` — both must return at least 1
4. `grep "CLAM-01" .planning/REQUIREMENTS.md` — must show `[x]` and `Complete`
5. No .cs files modified: `git diff --name-only` after execution shows only .yml and .md files
</verification>

<success_criteria>
- Compose files align with what ClamAVScannerService.GetEffectiveEndpointAsync reads (CLAMAV_HOST, CLAMAV_PORT single underscore)
- ENABLE_METRICS documented in both compose files as commented example
- All v1.2 requirements marked complete in REQUIREMENTS.md
- Zero code changes — this is purely a documentation/deployment-example fix
</success_criteria>

<output>
After completion, create `.planning/phases/12-clamav-ioptions-refactor-doc-fixes/12-01-SUMMARY.md`
</output>
