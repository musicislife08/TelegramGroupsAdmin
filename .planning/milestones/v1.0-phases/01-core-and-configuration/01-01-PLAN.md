---
phase: 01-core-and-configuration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - TelegramGroupsAdmin.Core/Extensions/EnumExtensions.cs
  - TelegramGroupsAdmin.Configuration/TelegramOptions.cs
  - TelegramGroupsAdmin.Configuration/OpenAIOptions.cs
  - TelegramGroupsAdmin.Configuration/SendGridOptions.cs
  - TelegramGroupsAdmin.Configuration/EmailOptions.cs
  - TelegramGroupsAdmin.Configuration/ConfigurationExtensions.cs
autonomous: true
requirements: [FILE-01, FILE-02, FILE-03, FILE-04, FILE-05, CMNT-02]

must_haves:
  truths:
    - "EnumExtensions.cs no longer exists in the repository"
    - "All four dead Configuration options files (TelegramOptions, OpenAIOptions, SendGridOptions, EmailOptions) no longer exist"
    - "The transition comment in ConfigurationExtensions.cs is gone while the surrounding code remains intact"
    - "The solution builds with zero errors after all deletions"
  artifacts:
    - path: "TelegramGroupsAdmin.Core/Extensions/EnumExtensions.cs"
      provides: "Must NOT exist (deleted)"
    - path: "TelegramGroupsAdmin.Configuration/TelegramOptions.cs"
      provides: "Must NOT exist (deleted)"
    - path: "TelegramGroupsAdmin.Configuration/OpenAIOptions.cs"
      provides: "Must NOT exist (deleted)"
    - path: "TelegramGroupsAdmin.Configuration/SendGridOptions.cs"
      provides: "Must NOT exist (deleted)"
    - path: "TelegramGroupsAdmin.Configuration/EmailOptions.cs"
      provides: "Must NOT exist (deleted)"
    - path: "TelegramGroupsAdmin.Configuration/ConfigurationExtensions.cs"
      provides: "Stale comment removed, code intact"
      contains: "AddApplicationConfiguration"
  key_links:
    - from: "ConfigurationExtensions.cs"
      to: "Program.cs / DI setup"
      via: "AddApplicationConfiguration still called"
      pattern: "AddApplicationConfiguration"
---

<objective>
Delete 5 dead files from Core and Configuration projects and remove 1 stale transition comment from ConfigurationExtensions.cs.

Purpose: First phase of dead code removal (#396). These files are confirmed dead by multi-agent audit -- EnumExtensions has zero callers, the four Configuration options classes are superseded by database-driven config, and the comment documents a transition that is long complete.

Output: 5 files deleted, 1 file edited, solution compiles cleanly.
</objective>

<execution_context>
@/Users/keisenmenger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/keisenmenger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead files and remove stale comment</name>
  <files>
    TelegramGroupsAdmin.Core/Extensions/EnumExtensions.cs,
    TelegramGroupsAdmin.Configuration/TelegramOptions.cs,
    TelegramGroupsAdmin.Configuration/OpenAIOptions.cs,
    TelegramGroupsAdmin.Configuration/SendGridOptions.cs,
    TelegramGroupsAdmin.Configuration/EmailOptions.cs,
    TelegramGroupsAdmin.Configuration/ConfigurationExtensions.cs
  </files>
  <action>
    1. Delete these 5 files using `git rm`:
       - TelegramGroupsAdmin.Core/Extensions/EnumExtensions.cs (FILE-01: unused generic GetDisplayName extension)
       - TelegramGroupsAdmin.Configuration/TelegramOptions.cs (FILE-02: self-documents as no longer used)
       - TelegramGroupsAdmin.Configuration/OpenAIOptions.cs (FILE-03: superseded by database config)
       - TelegramGroupsAdmin.Configuration/SendGridOptions.cs (FILE-04: superseded by database config)
       - TelegramGroupsAdmin.Configuration/EmailOptions.cs (FILE-05: never registered in DI)

    2. Edit TelegramGroupsAdmin.Configuration/ConfigurationExtensions.cs (CMNT-02):
       Remove lines 20-21 (the transition comment block):
       ```
       // NOTE: OpenAIOptions and SendGridOptions removed - now using database config
       // See AIProviderConfig and SendGridConfig in database (configs table)
       ```
       Leave a single blank line between line 19 (`services.Configure<ContentDetectionOptions>(...)`) and line 23 (`// Configuration repositories...`). The resulting file must have the same structure minus those two comment lines.
  </action>
  <verify>
    <automated>cd /Users/keisenmenger/Repos/personal/TelegramGroupsAdmin && ! test -f TelegramGroupsAdmin.Core/Extensions/EnumExtensions.cs && ! test -f TelegramGroupsAdmin.Configuration/TelegramOptions.cs && ! test -f TelegramGroupsAdmin.Configuration/OpenAIOptions.cs && ! test -f TelegramGroupsAdmin.Configuration/SendGridOptions.cs && ! test -f TelegramGroupsAdmin.Configuration/EmailOptions.cs && ! grep -q "OpenAIOptions and SendGridOptions removed" TelegramGroupsAdmin.Configuration/ConfigurationExtensions.cs && echo "PASS: All files deleted, comment removed" || echo "FAIL"</automated>
  </verify>
  <done>5 dead files deleted from disk and git index. Transition comment removed from ConfigurationExtensions.cs. File structure intact.</done>
</task>

<task type="auto">
  <name>Task 2: Verify solution builds cleanly</name>
  <files>TelegramGroupsAdmin.sln</files>
  <action>
    Run `dotnet build TelegramGroupsAdmin.sln` from the repository root. The build must complete with zero errors and zero warnings related to the deleted files.

    If the build fails:
    - If a file references EnumExtensions, TelegramOptions, OpenAIOptions, SendGridOptions, or EmailOptions -- that means the audit missed a reference. Investigate and remove the reference (these are confirmed dead, so any remaining reference is itself dead code or a using statement that can be removed).
    - If ConfigurationExtensions.cs fails to compile, the comment removal damaged surrounding code. Re-read the file and fix.

    IMPORTANT per CLAUDE.md: Do NOT run the app in normal mode. Only `dotnet build` to validate compilation.
  </action>
  <verify>
    <automated>cd /Users/keisenmenger/Repos/personal/TelegramGroupsAdmin && dotnet build TelegramGroupsAdmin.sln --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>Solution builds with zero errors. No references to deleted files remain.</done>
</task>

</tasks>

<verification>
1. `dotnet build TelegramGroupsAdmin.sln` passes with zero errors
2. `git status` shows 5 deleted files and 1 modified file
3. No file in the repository imports or references EnumExtensions, TelegramOptions, OpenAIOptions, SendGridOptions, or EmailOptions
</verification>

<success_criteria>
- EnumExtensions.cs does not exist in the repository
- TelegramOptions.cs, OpenAIOptions.cs, SendGridOptions.cs, EmailOptions.cs do not exist in the repository
- ConfigurationExtensions.cs compiles without the transition comment
- `dotnet build` succeeds with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-and-configuration/01-01-SUMMARY.md`
</output>
