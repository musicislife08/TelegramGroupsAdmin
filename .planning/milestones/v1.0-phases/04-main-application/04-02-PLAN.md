---
phase: 04-main-application
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - TelegramGroupsAdmin.ComponentTests/Components/TelegramPreviewTests.cs
  - TelegramGroupsAdmin.ComponentTests/Components/TelegramBotMessageTests.cs
  - TelegramGroupsAdmin.ComponentTests/Components/TelegramUserMessageTests.cs
  - TelegramGroupsAdmin.ComponentTests/Components/TelegramReturnButtonTests.cs
autonomous: true
requirements: [TEST-01]

must_haves:
  truths:
    - "Test files for the four deleted Razor components no longer exist"
    - "The test project compiles cleanly after test file deletion"
    - "dotnet build passes with zero errors for the entire solution"
  artifacts: []
  key_links: []
---

<objective>
Delete the four orphaned component test files that tested the Razor components removed in Plan 01, and verify the full solution builds cleanly.

Purpose: Complete the dead code removal for Phase 4 by cleaning up orphaned tests that would cause build failures referencing deleted components.
Output: 4 test files deleted, full solution build passes.
</objective>

<execution_context>
@/Users/keisenmenger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/keisenmenger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-main-application/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete 4 orphaned component test files</name>
  <files>
    TelegramGroupsAdmin.ComponentTests/Components/TelegramPreviewTests.cs,
    TelegramGroupsAdmin.ComponentTests/Components/TelegramBotMessageTests.cs,
    TelegramGroupsAdmin.ComponentTests/Components/TelegramUserMessageTests.cs,
    TelegramGroupsAdmin.ComponentTests/Components/TelegramReturnButtonTests.cs
  </files>
  <action>
Delete all 4 test files using `git rm`:

```
git rm TelegramGroupsAdmin.ComponentTests/Components/TelegramPreviewTests.cs \
       TelegramGroupsAdmin.ComponentTests/Components/TelegramBotMessageTests.cs \
       TelegramGroupsAdmin.ComponentTests/Components/TelegramUserMessageTests.cs \
       TelegramGroupsAdmin.ComponentTests/Components/TelegramReturnButtonTests.cs
```

These test files test the four Razor components deleted in Plan 01 (TelegramPreview, TelegramBotMessage, TelegramUserMessage, TelegramReturnButton). With the components gone, these tests are orphaned dead code.
  </action>
  <verify>
    <automated>test ! -f TelegramGroupsAdmin.ComponentTests/Components/TelegramPreviewTests.cs && test ! -f TelegramGroupsAdmin.ComponentTests/Components/TelegramBotMessageTests.cs && test ! -f TelegramGroupsAdmin.ComponentTests/Components/TelegramUserMessageTests.cs && test ! -f TelegramGroupsAdmin.ComponentTests/Components/TelegramReturnButtonTests.cs && echo "All 4 test files deleted"</automated>
  </verify>
  <done>All 4 orphaned component test files are removed from the working tree and staged for commit</done>
</task>

<task type="auto">
  <name>Task 2: Verify full solution build</name>
  <files></files>
  <action>
Run a full solution build to confirm that all Phase 4 deletions (Plans 01 + 02) leave the solution in a clean compiling state:

```
dotnet build
```

If build fails, diagnose and fix. Common issues from dead code deletion:
- Missing using directives (should not happen since deleted files had zero references)
- Test project referencing deleted component namespaces (should be resolved by test file deletion)

Also run unit tests to confirm no test regressions:
```
dotnet test TelegramGroupsAdmin.UnitTests --no-build
```
  </action>
  <verify>
    <automated>cd /Users/keisenmenger/Repos/personal/TelegramGroupsAdmin && dotnet build -v q 2>&1 | tail -5</automated>
  </verify>
  <done>Full solution builds with zero errors and zero warnings related to deleted code. Unit tests pass.</done>
</task>

</tasks>

<verification>
- `dotnet build` passes with zero errors
- `dotnet test TelegramGroupsAdmin.UnitTests --no-build` passes
- No test files reference TelegramPreview, TelegramBotMessage, TelegramUserMessage, or TelegramReturnButton
</verification>

<success_criteria>
- 4 orphaned component test files deleted
- Full solution builds cleanly with zero errors
- Unit tests pass with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/04-main-application/04-02-SUMMARY.md`
</output>
