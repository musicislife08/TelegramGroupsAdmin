---
phase: 03-telegram-project
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - TelegramGroupsAdmin.Telegram/Services/IMessageQueryService.cs
  - TelegramGroupsAdmin.Telegram/Services/MessageQueryService.cs
  - TelegramGroupsAdmin.Core/Services/IJobTriggerService.cs
  - TelegramGroupsAdmin.BackgroundJobs/Services/JobTriggerService.cs
  - TelegramGroupsAdmin.Core/BackgroundJobs/IJobScheduler.cs
  - TelegramGroupsAdmin.BackgroundJobs/Services/QuartzJobScheduler.cs
  - TelegramGroupsAdmin.Telegram/Services/Bot/IBotMediaService.cs
  - TelegramGroupsAdmin.Telegram/Services/Bot/BotMediaService.cs
  - TelegramGroupsAdmin.Telegram/Extensions/TelegramLoggingExtensions.cs
  - TelegramGroupsAdmin.BackgroundJobs/Helpers/JobPayloadHelper.cs
  - TelegramGroupsAdmin.UnitTests/BackgroundJobs/Helpers/JobPayloadHelperTests.cs
  - TelegramGroupsAdmin.UnitTests/Telegram/Services/Bot/BotMediaServiceTests.cs
  - TelegramGroupsAdmin.IntegrationTests/Repositories/MessageHistoryRepositoryTests.cs
autonomous: true
requirements: [MTD-01, MTD-02, MTD-03, MTD-04, MTD-05, MTD-06, MTD-07, MTD-08, MTD-09, MTD-10, MTD-11, TEST-02, TEST-03, TEST-04]

must_haves:
  truths:
    - "IMessageQueryService no longer declares GetMessagesBeforeAsync, GetMessagesByDateRangeAsync, GetDistinctUserNamesAsync, or GetDistinctChatNamesAsync"
    - "IJobTriggerService no longer declares ScheduleOnceAsync or CancelScheduledJobAsync"
    - "IJobScheduler no longer declares IsScheduledAsync"
    - "IBotMediaService no longer declares DownloadFileAsBytesAsync"
    - "TelegramLoggingExtensions no longer contains GetUserLogDisplayAsync or GetChatLogDisplayAsync"
    - "JobPayloadHelper no longer contains GetRequiredPayload method"
    - "Test classes for GetRequiredPayload, DownloadFileAsBytesAsync, and the dead IMessageQueryService methods no longer exist"
    - "dotnet build passes with zero errors after all removals"
  artifacts:
    - path: "TelegramGroupsAdmin.Telegram/Services/IMessageQueryService.cs"
      provides: "Interface with 4 dead methods removed, live methods preserved"
      contains: "GetRecentMessagesAsync"
    - path: "TelegramGroupsAdmin.Telegram/Services/MessageQueryService.cs"
      provides: "Implementation with 4 dead methods removed"
      contains: "GetRecentMessagesAsync"
    - path: "TelegramGroupsAdmin.Core/Services/IJobTriggerService.cs"
      provides: "Interface with 2 dead methods removed, TriggerNowAsync preserved"
      contains: "TriggerNowAsync"
    - path: "TelegramGroupsAdmin.BackgroundJobs/Services/JobTriggerService.cs"
      provides: "Implementation with 2 dead methods removed"
      contains: "TriggerNowAsync"
    - path: "TelegramGroupsAdmin.Core/BackgroundJobs/IJobScheduler.cs"
      provides: "Interface with IsScheduledAsync removed, ScheduleJobAsync and CancelJobAsync preserved"
      contains: "ScheduleJobAsync"
    - path: "TelegramGroupsAdmin.BackgroundJobs/Services/QuartzJobScheduler.cs"
      provides: "Implementation with IsScheduledAsync removed"
      contains: "ScheduleJobAsync"
    - path: "TelegramGroupsAdmin.Telegram/Services/Bot/IBotMediaService.cs"
      provides: "Interface with DownloadFileAsBytesAsync removed, 5 live methods preserved"
      contains: "GetUserPhotoAsync"
    - path: "TelegramGroupsAdmin.Telegram/Services/Bot/BotMediaService.cs"
      provides: "Implementation with DownloadFileAsBytesAsync removed"
      contains: "GetUserPhotoAsync"
    - path: "TelegramGroupsAdmin.Telegram/Extensions/TelegramLoggingExtensions.cs"
      provides: "Extension class with 2 async repository extensions removed, all sync extensions preserved"
      contains: "extension(User? user)"
    - path: "TelegramGroupsAdmin.BackgroundJobs/Helpers/JobPayloadHelper.cs"
      provides: "Helper class with GetRequiredPayload removed, TryGetPayloadAsync preserved"
      contains: "TryGetPayloadAsync"
  key_links:
    - from: "IMessageQueryService.cs"
      to: "MessageQueryService.cs"
      via: "interface contract"
      pattern: "IMessageQueryService"
    - from: "IJobTriggerService.cs"
      to: "JobTriggerService.cs"
      via: "interface contract"
      pattern: "IJobTriggerService"
    - from: "IJobScheduler.cs"
      to: "QuartzJobScheduler.cs"
      via: "interface contract"
      pattern: "IJobScheduler"
    - from: "IBotMediaService.cs"
      to: "BotMediaService.cs"
      via: "interface contract"
      pattern: "IBotMediaService"
---

<objective>
Remove 11 dead methods from 6 interfaces/classes and delete 3 groups of orphaned tests.

Purpose: Surgically remove dead method declarations from interfaces AND their implementations, plus delete the test methods that only exercise the dead code. Each interface has live methods that MUST be preserved.
Output: 10 production files edited (method removals), 3 test files edited (orphaned test removals).
</objective>

<execution_context>
@/Users/keisenmenger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/keisenmenger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- These interfaces have BOTH live and dead methods. Only remove the dead ones listed. -->

From TelegramGroupsAdmin.Telegram/Services/IMessageQueryService.cs:
KEEP: GetRecentMessagesAsync, GetMessagesByChatIdAsync, GetMessagesWithDetectionHistoryAsync,
      GetMessageWithDetectionHistoryAsync, GetContentChecksForMessagesAsync, GetUserRecentPhotoAsync,
      GetMessageByIdAsync, GetUserMessagesPaginatedAsync
REMOVE: GetMessagesBeforeAsync, GetMessagesByDateRangeAsync, GetDistinctUserNamesAsync, GetDistinctChatNamesAsync

From TelegramGroupsAdmin.Core/Services/IJobTriggerService.cs:
KEEP: TriggerNowAsync
REMOVE: ScheduleOnceAsync, CancelScheduledJobAsync

From TelegramGroupsAdmin.Core/BackgroundJobs/IJobScheduler.cs:
KEEP: ScheduleJobAsync, CancelJobAsync
REMOVE: IsScheduledAsync

From TelegramGroupsAdmin.Telegram/Services/Bot/IBotMediaService.cs:
KEEP: GetUserPhotoAsync, GetChatIconAsync, GetFileAsync, DownloadFileAsync, GetUserProfilePhotosAsync
REMOVE: DownloadFileAsBytesAsync

From TelegramGroupsAdmin.Telegram/Extensions/TelegramLoggingExtensions.cs:
KEEP: All sync extension blocks (extension(User?), extension(Chat?), extension(TelegramUserDto?),
      extension(TelegramUser?), extension(MessageRecord?))
REMOVE: extension(ITelegramUserRepository) block containing GetUserLogDisplayAsync,
        extension(IManagedChatsRepository) block containing GetChatLogDisplayAsync

From TelegramGroupsAdmin.BackgroundJobs/Helpers/JobPayloadHelper.cs:
KEEP: TryGetPayloadAsync, CleanupStaleTriggerAsync (private)
REMOVE: GetRequiredPayload
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead methods from interfaces and implementations</name>
  <files>
    TelegramGroupsAdmin.Telegram/Services/IMessageQueryService.cs,
    TelegramGroupsAdmin.Telegram/Services/MessageQueryService.cs,
    TelegramGroupsAdmin.Core/Services/IJobTriggerService.cs,
    TelegramGroupsAdmin.BackgroundJobs/Services/JobTriggerService.cs,
    TelegramGroupsAdmin.Core/BackgroundJobs/IJobScheduler.cs,
    TelegramGroupsAdmin.BackgroundJobs/Services/QuartzJobScheduler.cs,
    TelegramGroupsAdmin.Telegram/Services/Bot/IBotMediaService.cs,
    TelegramGroupsAdmin.Telegram/Services/Bot/BotMediaService.cs,
    TelegramGroupsAdmin.Telegram/Extensions/TelegramLoggingExtensions.cs,
    TelegramGroupsAdmin.BackgroundJobs/Helpers/JobPayloadHelper.cs
  </files>
  <action>
For each interface/implementation pair, remove ONLY the dead methods listed below. Preserve all other methods. Remove the method signature (including XML doc comments) from the interface AND the method body from the implementation.

**IMessageQueryService + MessageQueryService (MTD-01..04):**
Remove these 4 methods from both interface and implementation:
- `GetMessagesBeforeAsync` (cursor-based pagination -- dead)
- `GetMessagesByDateRangeAsync` (date range query -- dead)
- `GetDistinctUserNamesAsync` (distinct usernames -- dead)
- `GetDistinctChatNamesAsync` (distinct chat names -- dead)

Also remove the `using UiModels = TelegramGroupsAdmin.Telegram.Models;` alias from IMessageQueryService.cs ONLY if it becomes unused after removing the dead methods. Check first: the remaining methods use `UiModels.MessageRecord`, `UiModels.MessageWithDetectionHistory`, `UiModels.ContentCheckRecord`, `UiModels.PhotoMessageRecord` -- so the alias is still needed. Keep it.

**IJobTriggerService + JobTriggerService (MTD-05..06):**
Remove these 2 methods from both interface and implementation:
- `ScheduleOnceAsync` (scheduled one-time execution -- dead)
- `CancelScheduledJobAsync` (cancel scheduled job -- dead)
Keep: `TriggerNowAsync` (live, used by background jobs)

**IJobScheduler + QuartzJobScheduler (MTD-07):**
Remove this 1 method from both interface and implementation:
- `IsScheduledAsync` (check if job is scheduled -- dead)
Keep: `ScheduleJobAsync`, `CancelJobAsync` (both live)

**IBotMediaService + BotMediaService (MTD-08):**
Remove this 1 method from both interface and implementation:
- `DownloadFileAsBytesAsync` (convenience byte array download -- dead)
Keep: `GetUserPhotoAsync`, `GetChatIconAsync`, `GetFileAsync`, `DownloadFileAsync`, `GetUserProfilePhotosAsync` (all live)

**TelegramLoggingExtensions (MTD-09..10):**
Remove these 2 entire extension blocks (they are the async repository-based extensions at the bottom of the file):
- `extension(ITelegramUserRepository repo)` block containing `GetUserLogDisplayAsync` (lines ~145-164)
- `extension(IManagedChatsRepository repo)` block containing `GetChatLogDisplayAsync` (lines ~166-185)
Keep: All 5 sync extension blocks above them (extension(User?), extension(Chat?), extension(TelegramUserDto?), extension(TelegramUser?), extension(MessageRecord?))

After removing the two async blocks, also remove any `using` directives that become unused. The `using TelegramGroupsAdmin.Telegram.Repositories;` import is ONLY used by the async extensions (ITelegramUserRepository, IManagedChatsRepository). Remove it. Check `using TelegramGroupsAdmin.Core.Utilities;` -- it provides `LogDisplayName` which is used by the remaining sync extensions. Keep it.

**JobPayloadHelper (MTD-11):**
Remove this 1 method:
- `GetRequiredPayload<T>` (throws on missing payload -- dead; all callers migrated to TryGetPayloadAsync)
Keep: `TryGetPayloadAsync<T>`, `CleanupStaleTriggerAsync` (both live)
  </action>
  <verify>
    <automated>cd /Users/keisenmenger/Repos/personal/TelegramGroupsAdmin && dotnet build --no-restore TelegramGroupsAdmin.sln</automated>
  </verify>
  <done>11 dead methods removed from 6 interfaces/classes and their implementations. All remaining live methods intact. Solution builds with zero errors.</done>
</task>

<task type="auto">
  <name>Task 2: Remove orphaned test methods for dead code</name>
  <files>
    TelegramGroupsAdmin.UnitTests/BackgroundJobs/Helpers/JobPayloadHelperTests.cs,
    TelegramGroupsAdmin.UnitTests/Telegram/Services/Bot/BotMediaServiceTests.cs,
    TelegramGroupsAdmin.IntegrationTests/Repositories/MessageHistoryRepositoryTests.cs
  </files>
  <action>
Remove test methods that exercise the dead code removed in Task 1. Each test file has LIVE tests that must be preserved.

**JobPayloadHelperTests.cs (TEST-02):**
Remove the entire `#region GetRequiredPayload Tests` block (3 test methods):
- `GetRequiredPayload_ValidPayload_ReturnsDeserializedObject`
- `GetRequiredPayload_MissingPayload_ThrowsKeyNotFoundException`
- `GetRequiredPayload_InvalidJson_ThrowsJsonException`
Keep: All `#region TryGetPayloadAsync Tests` (6 test methods), the TestPayload record, SetUp, class definition.

**BotMediaServiceTests.cs (TEST-03):**
Remove 2 test methods from the `#region Passthrough Method Tests` region:
- `DownloadFileAsBytesAsync_ReturnsFileContents` (~line 325-345)
- `DownloadFileAsBytesAsync_NoFilePath_ThrowsException` (~line 348-358)
Keep: `GetFileAsync_PassesThroughToHandler` in the same region, plus ALL tests in other regions (cache hit, cache invalidation, cache miss, chat icon tests). The `#region Passthrough Method Tests` region should remain with just the GetFileAsync test inside it. If only 1 test remains, you may remove the region markers or keep them -- either is fine.

**MessageHistoryRepositoryTests.cs (TEST-04):**
Remove 5 test methods that exercise the 4 dead IMessageQueryService methods:
- `GetMessagesBeforeAsync_WithNoTimestamp_ShouldReturnRecentMessages` (~line 220)
- `GetMessagesBeforeAsync_WithTimestamp_ShouldReturnMessagesBeforeCursor` (~line 232)
- `GetMessagesByDateRangeAsync_ShouldFilterByDateRange` (~line 340)
- `GetDistinctUserNamesAsync_ShouldReturnUserNames` (~line 1400)
- `GetDistinctChatNamesAsync_ShouldReturnChatNames` (~line 1415)
Keep: ALL other tests in the file (GetRecentMessagesAsync, GetMessagesByChatIdAsync, GetMessagesWithDetectionHistoryAsync, GetContentChecksForMessagesAsync, GetUserRecentPhotoAsync, GetMessageByIdAsync, GetUserMessagesPaginatedAsync, etc.)
  </action>
  <verify>
    <automated>cd /Users/keisenmenger/Repos/personal/TelegramGroupsAdmin && dotnet build --no-restore TelegramGroupsAdmin.sln</automated>
  </verify>
  <done>Orphaned test methods removed from 3 test files. All remaining live tests compile. Solution builds with zero errors.</done>
</task>

</tasks>

<verification>
- `dotnet build` passes with zero errors
- Grep confirms no declarations of removed methods remain in interfaces
- All test files compile and remaining tests are intact
</verification>

<success_criteria>
- 11 dead method declarations removed from interfaces
- 11 corresponding implementations removed
- 10 orphaned test methods removed from 3 test files
- All live methods and tests preserved
- Solution compiles cleanly with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-telegram-project/03-02-SUMMARY.md`
</output>
