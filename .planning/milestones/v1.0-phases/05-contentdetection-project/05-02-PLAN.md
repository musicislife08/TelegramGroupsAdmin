---
phase: 05-contentdetection-project
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - TelegramGroupsAdmin.ContentDetection/Models/ContentCheckRequest.cs
  - TelegramGroupsAdmin.ContentDetection/Models/ImageCheckRequest.cs
  - TelegramGroupsAdmin.ContentDetection/Services/ScanResultType.cs
  - TelegramGroupsAdmin.ContentDetection/Services/ContentDetectionEngineV2.cs
  - TelegramGroupsAdmin.ContentDetection/Checks/VideoContentCheckV2.cs
  - TelegramGroupsAdmin.ContentDetection/Checks/ImageContentCheckV2.cs
  - TelegramGroupsAdmin.UnitTests/Configuration/ContentDetectionConfigMappingsTests.cs
autonomous: true
requirements: [PROP-01, PROP-02, PROP-03, PROP-04, ENUM-01, TEST-05]

must_haves:
  truths:
    - "ContentCheckRequest no longer has CheckOnly, ImageFileName, or PhotoUrl properties"
    - "ImageCheckRequest no longer has PhotoUrl property"
    - "ScanResultType enum no longer has a Suspicious value"
    - "All switch expressions and pattern matches over ScanResultType still compile"
    - "The orphaned CasConfig region in ContentDetectionConfigMappingsTests.cs is removed"
    - "dotnet build passes with zero errors"
    - "dotnet test on UnitTests project passes"
  artifacts:
    - path: "TelegramGroupsAdmin.ContentDetection/Models/ContentCheckRequest.cs"
      provides: "Record with CheckOnly, ImageFileName, PhotoUrl removed"
    - path: "TelegramGroupsAdmin.ContentDetection/Models/ImageCheckRequest.cs"
      provides: "Class with PhotoUrl removed"
    - path: "TelegramGroupsAdmin.ContentDetection/Services/ScanResultType.cs"
      provides: "Enum with Suspicious value removed"
    - path: "TelegramGroupsAdmin.UnitTests/Configuration/ContentDetectionConfigMappingsTests.cs"
      provides: "Test file with orphaned CasConfig region removed, all remaining tests intact"
  key_links:
    - from: "ContentCheckRequest"
      to: "ContentDetectionEngineV2.cs"
      via: "object initializer"
      pattern: "Must remove PhotoUrl = originalRequest.PhotoUrl from ImageCheckRequest construction"
    - from: "ContentCheckRequest"
      to: "VideoContentCheckV2.cs, ImageContentCheckV2.cs"
      via: "object initializer"
      pattern: "Must remove CheckOnly and PhotoUrl from ContentCheckRequest construction at call sites"
    - from: "ScanResultType"
      to: "ClamAVScannerService, Tier1VotingCoordinator"
      via: "enum usage"
      pattern: "No switch/match references Suspicious — safe removal"
---

<objective>
Remove 4 dead properties from content check request types, remove the dead ScanResultType.Suspicious enum value, and remove the orphaned CasConfig tests from ContentDetectionConfigMappingsTests.cs.

Purpose: Complete the ContentDetection project dead code cleanup by removing dead properties that are set but never read, a dead enum value, and misplaced test methods.
Output: 7 files modified with dead code removed.
</objective>

<execution_context>
@/Users/keisenmenger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/keisenmenger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead properties from ContentCheckRequest and ImageCheckRequest, plus dead enum value and orphaned tests</name>
  <files>
    TelegramGroupsAdmin.ContentDetection/Models/ContentCheckRequest.cs
    TelegramGroupsAdmin.ContentDetection/Models/ImageCheckRequest.cs
    TelegramGroupsAdmin.ContentDetection/Services/ScanResultType.cs
    TelegramGroupsAdmin.ContentDetection/Services/ContentDetectionEngineV2.cs
    TelegramGroupsAdmin.ContentDetection/Checks/VideoContentCheckV2.cs
    TelegramGroupsAdmin.ContentDetection/Checks/ImageContentCheckV2.cs
    TelegramGroupsAdmin.UnitTests/Configuration/ContentDetectionConfigMappingsTests.cs
  </files>
  <action>
**ContentCheckRequest.cs — Remove 3 dead properties (PROP-01, PROP-02, PROP-03):**

1. Remove `CheckOnly` property (line 33, `public bool CheckOnly { get; init; } = false;` and its XML doc). Never read by any consumer. Set at write-only call sites that must also be cleaned.
2. Remove `ImageFileName` property (line 58, `public string? ImageFileName { get; init; }` and its XML doc). Zero references — never set, never read.
3. Remove `PhotoUrl` property (line 73, `public string? PhotoUrl { get; init; }` and its XML doc). Only set in write-only call sites, never read by any consumer.

**ImageCheckRequest.cs — Remove 1 dead property (PROP-04):**

Remove `PhotoUrl` property (line 9, `public required string? PhotoUrl { get; init; }`). Only set in ContentDetectionEngineV2.cs when constructing ImageCheckRequest, but never read by ImageContentCheckV2 (the check that processes image requests). After removing from ImageCheckRequest, clean up the call site.

**Clean up write-only set-sites after property removal:**

After removing properties from the types, remove the corresponding property assignments at call sites:

1. `ContentDetectionEngineV2.cs` line 379: Remove `PhotoUrl = originalRequest.PhotoUrl,` from the ImageCheckRequest construction (around line 373-383). The `PhotoUrl` property no longer exists on ImageCheckRequest.

2. `VideoContentCheckV2.cs` lines 356, 362: Remove `CheckOnly = false,` and `PhotoUrl = null,` from the ContentCheckRequest construction (around lines 350-366).

3. `ImageContentCheckV2.cs` lines 188, 194: Remove `CheckOnly = false,` and `PhotoUrl = null,` from the ContentCheckRequest construction (around lines 182-197).

**ScanResultType.cs — Remove dead enum value (ENUM-01):**

Remove the `Suspicious = 2` value and its XML doc comment (lines 18-22). This value is never referenced anywhere in the codebase (confirmed by grep). The remaining values are Clean=0, Infected=1, Error=3, Skipped=4. DO NOT renumber the other enum values — they may be stored in databases or serialized.

After removal, verify no switch expression or pattern match references `ScanResultType.Suspicious` (already confirmed: zero references exist).

**ContentDetectionConfigMappingsTests.cs — Remove orphaned test region (TEST-05):**

Remove the entire `#region CasConfig Mappings` block (lines 16-93). These 3 test methods test CasConfig/CasConfigData mappings from WelcomeConfigMappings, but are placed in ContentDetectionConfigMappingsTests.cs. They are orphaned relative to this test file's scope.

The 3 orphaned tests being removed:
- `CasConfigData_ToModel_ConvertsSecondsToTimeSpan`
- `CasConfig_ToData_ConvertsTimeSpanToSeconds`
- `CasConfig_RoundTrip_PreservesTimeSpan`

Keep ALL other test regions (UrlBlocklistConfig, ThreatIntelConfig, SeoScrapingConfig, ImageContentConfig, VideoContentConfig, Edge Cases, Full ContentDetectionConfig Round-Trip). These test real ContentDetectionConfigMappings code.

Also remove the `using TelegramGroupsAdmin.Configuration.Models.Welcome;` import at line 3 ONLY IF no remaining tests reference Welcome config types. Check: CasConfig is in the Welcome namespace. If the CasConfig region is the only consumer of that import, remove it. Also check if the `using TelegramGroupsAdmin.Data.Models.Configs;` import line 4 is still needed (CasConfigData is in that namespace but may be used by other config data types too — UrlBlocklistConfigData etc. are also there, so the import stays).

After all changes, run `dotnet build` and then run unit tests to verify everything compiles and passes.
  </action>
  <verify>
    <automated>cd /Users/keisenmenger/Repos/personal/TelegramGroupsAdmin && dotnet build --verbosity quiet && dotnet test TelegramGroupsAdmin.UnitTests --no-build --verbosity quiet</automated>
  </verify>
  <done>4 dead properties removed from content check request types with all write-only set-sites cleaned. ScanResultType.Suspicious removed. 3 orphaned CasConfig tests removed. Solution builds and all remaining tests pass.</done>
</task>

</tasks>

<verification>
After task completion:
1. `dotnet build` passes with zero errors
2. Grep confirms ContentCheckRequest no longer has CheckOnly, ImageFileName, or PhotoUrl
3. Grep confirms ImageCheckRequest no longer has PhotoUrl
4. Grep confirms ScanResultType no longer has Suspicious
5. Grep confirms no CasConfig test methods in ContentDetectionConfigMappingsTests.cs
6. Unit tests pass
</verification>

<success_criteria>
- ContentCheckRequest.CheckOnly, ContentCheckRequest.ImageFileName, ContentCheckRequest.PhotoUrl removed
- ImageCheckRequest.PhotoUrl removed
- All write-only set-sites for removed properties cleaned from ContentDetectionEngineV2, VideoContentCheckV2, ImageContentCheckV2
- ScanResultType.Suspicious enum value removed (without renumbering others)
- 3 orphaned CasConfig test methods removed from ContentDetectionConfigMappingsTests.cs
- Solution compiles with zero errors and all remaining unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-contentdetection-project/05-02-SUMMARY.md`
</output>
