name: Validate PR Target

on:
  pull_request:
    branches:
      - master

jobs:
  validate-pr-source:
    name: Validate PR Source Branch
    runs-on: ubuntu-latest

    steps:
      - name: Check PR source branch
        env:
          SOURCE_BRANCH: ${{ github.head_ref }}
        run: |
          echo "🔍 Validating PR source branch..."
          echo "Source branch: $SOURCE_BRANCH"
          echo "Target branch: master"
          echo ""

          # Only allow develop → master PRs
          if [[ "$SOURCE_BRANCH" != "develop" ]]; then
            echo "❌ ERROR: Pull requests to master must come from 'develop' branch"
            echo ""
            echo "Current source: $SOURCE_BRANCH"
            echo "Required source: develop"
            echo ""
            echo "📋 Correct workflow:"
            echo "1. Create PR: $SOURCE_BRANCH → develop"
            echo "2. Merge to develop after tests pass"
            echo "3. Then create PR: develop → master"
            echo ""
            echo "This ensures all changes go through the develop branch for testing before reaching master."
            exit 1
          fi

          echo "✅ PR source validation passed - develop → master is allowed"
